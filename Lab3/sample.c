#include <stdio.h>
#define MAX 100

// This is a single-line comment
int main() {
    int x = 10;   // initialize x
    int y = 20;

    /* This is a
       multi-line comment */
    int sum = x + y;

    printf("The sum is: %d\n", sum);

    return 0;
}
